name: PR validation CI
on:
  pull_request:
    branches: [master, dev]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Go to correct path folder
        run: cd ../../alert-app-fe
      - name: Run build command
        run:  npm run build
